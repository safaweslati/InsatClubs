using InsatClub.Models;

namespace InsatClub.Data.Repository
{
  /*  public class EventRepository : Repository<Event> , IEventRepository 
    {
        public EventRepository(ClubsInsatContext _Context) : base(_Context)
        {
        }
        IEnumerable<Event> GetAllEventsForClub(int id)
        {
            var admin = this._Context.Administrateurs.Find(id);
            var club = admin.Club;
            return club.Events ;
         }
        void Update(int Id, Event e)
        {



        }
    }*/
}
